// Placeholder — l'app editor vera (viewport, hierarchy panel, inspector,
// PWA manifest + service worker) arriva in Fase 4. Questo file esiste solo
// per dare a tsc/vite qualcosa di valido da compilare.

export const EDITOR_PLACEHOLDER = true;
